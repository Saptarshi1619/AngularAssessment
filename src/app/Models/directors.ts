export class director{
    id:number
    position:string
    name:string
    bio:string

    constructor(id:number, position:string,
        name:string,
        bio:string){
            this.id = id
            this.position = position
            this.name = name
            this.bio = bio
    }
}

