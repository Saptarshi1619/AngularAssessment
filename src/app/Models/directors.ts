export class director{
    id:number
    position:string
    name:string
    bio:string
    img:string

    constructor(id:number, position:string,
        name:string,
        bio:string,
        img: string){
            this.id = id
            this.position = position
            this.name = name
            this.bio = bio
            this.img = img
    }
}

